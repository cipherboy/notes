# Vim

Switch to the next file:

    :next
    :n

Go to beginning of line:

    ^ -- relative (to whitespace)
    0/<HOME> -- absolute

Move based on words:

    b/B -- back a word
    w/W -- forward a word
